{% block stylesheets %}
<link rel="stylesheet" href="assets/css/base.css">
<link rel="stylesheet" href="../assets/css/base.css">
{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark container-fluid justify-content-end rounded-0">
        <a class="navbar-brand" href="{{ BASE_URL }}..\blog"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ BASE_URL }}posts">Articles</a>
                </li>
                {% if session['role'] == 'admin' %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Admin
                        </a>
                        <div class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown">
                            <a class="nav-link" href="{{ BASE_URL }}admin/comments">Commentaires</a>
                            <a class="nav-link" href="{{ BASE_URL }}admin/posts">Articles</a>
                            <div class="dropdown-divider" style="border-color:grey;"></div>
                            <a class="nav-link" href="{{ BASE_URL }}admin/user">Utilisateurs</a>
                        </div>
                    </li>
                {% endif %}
                {% if session == null %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ BASE_URL }}auth">Connexion</a>
                    </li>
                {% endif %}
                {% if session %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                        Mon compte
                        </a>
                        <div class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown">
                            <a class="nav-link" href="{{ BASE_URL }}user/account">Mes informations</a>                            
                        <div class="dropdown-divider" style="border-color:grey;"></div>
                            <a class="nav-link" href="{{ BASE_URL }}logout"> Déconnexion</a>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
{% endblock %}